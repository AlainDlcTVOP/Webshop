@model SkiShop.Models.Product

@{
    ViewData["Title"] = "Delete Product";
    Layout = "/Pages/Shared/_Layout.cshtml";
}

<div class="container-fluid">

    <h1>Delete Product</h1>

    <h3>Want to delete this?</h3>
    <div>
        <h4>Product</h4>
        <hr />
        <dl class="row">
            <dt class="col-sm-2">
                @Model.Name
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.Price)
            </dd>
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.Description)
            </dt>
           
            <dt class="col-sm-2">
                Uploaded Image
            </dt>

            @*  
               Cant get the Images from database 
            <dd class="col-sm-10">
            @if (Model. != null)
                {
                    <img src="@("~/images/"+Model.Image)" asp-append-version="true" width="250" height="250" />
                }
                else
                {
                    <img class="card" src="~/Images/example.jpg">
                }
             </dd>
            *@
        </dl>

        <form asp-action="Delete">
            <input type="hidden" asp-for="Id" />
            <input type="submit" value="Delete" class="btn btn-danger" /> |
            <a class="btn btn-primary" asp-action=" ">Back to List</a>
        </form>
    </div>

</div>