@model Product

@{
    Layout = null; // Prevent layout from being rendered twice
}

@if (User.Identity.IsAuthenticated && User.IsInRole("Admin"))
{
    <button type="button" class="btn btn-danger" onclick="DeleteProduct(@Model.Id)">Ta bort produkt</button>
}