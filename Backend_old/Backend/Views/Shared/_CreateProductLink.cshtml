
@{
    Layout = null; // Prevent layout from being rendered twice
}

@if (User.Identity.IsAuthenticated && User.IsInRole("Admin"))
{
    <button type="button" class="btn btn-primary" onclick="CreateProduct()">Skapa produkt</button>
}
